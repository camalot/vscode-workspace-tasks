if any files defined for workspaceTasks.envVars.envFiles or workspaceTasks.envVars.secretFiles and they are tracked files in git, it should report a warning in the PROBLEMS list. This should be backed by a configuration setting that allows users to disable the warning if they choose to. The warning should inform users about the potential security risks of tracking environment variable files in version control and recommend best practices for managing secrets.

For the Secrets treeview item, there should be a setting that allows the user to show the root item even if there are no secrets defined. This can be useful for users who want to have a consistent view of the Secrets section in the treeview, regardless of whether they have secrets defined or not. The setting should be a boolean that, when enabled, will display the root item in the treeview even if there are no secrets defined.
